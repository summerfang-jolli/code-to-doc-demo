[project]
name = "code-to-doc-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.1",
    "ragflow-sdk>=0.20.5",
    "requests>=2.32.5",
]
